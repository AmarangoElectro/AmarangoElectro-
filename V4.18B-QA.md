# V4.18B QA — FASE H Recovery

## Veredicto del gate

**BLOCKED — FASE H / V4.18B**

La implementación funcional dirigida está presente y sus pruebas específicas pasan, pero el gate no puede cerrarse como PASS porque la instalación reproducible quedó bloqueada por infraestructura de red. En consecuencia, `lint`, `build` y browser QA real no pueden declararse PASS.

## Separación de líneas

- Este trabajo pertenece exclusivamente a la línea **Recovery real** iniciada desde FASE G / V4.18A.
- **LAB Master App no fue tocada ni mezclada.** El último LAB físico indicado por el dueño sigue siendo V42.3 fuera de este paquete.
- No se abrió ningún gate posterior.

## Identidad física verificada

- ZIP base FASE G / V4.18A SHA-256: `cc504e5e5cb059767bf1018eaaf4740ca0b17d7695516b61c94745c117e72bd1` — PASS.
- Rama base: `recovery/amarango-v16-v418a-quick-actions-v1`.
- HEAD base: `1029c13a9656013a0a54f20ec8c400419358d641` — PASS.
- Commit implementación V4.18A: `456d5e807d21b0ea76244fe6cc49f9c05514f4d6` — presente en historia.
- Rama V4.18B: `recovery/amarango-v16-v418b-storefront-admin-mode-v1`.
- Commit implementación V4.18B: `f1fd12bf386ce5c3535ac7f699980a708e65361d`.
- `main`: ausente en el recovery local.

## Comportamiento implementado

1. Admin Mode se ofrece únicamente dentro de la superficie interna LAB de Administración.
2. No existe activación pública, por URL ni persistida.
3. Admin Mode OFF resuelve a proyección Cliente y `CatalogClient` retorna la misma `ProductCard` sin wrapper administrativo.
4. Admin Mode ON agrega una capa contextual discreta alrededor de la tarjeta sin modificar Product V16.
5. El control contextual abre `V418AQuickActionsSheet` existente.
6. Cliente y Asesor no reciben `adminOverlay`, controles V4.18B ni Quick Actions.
7. Desactivar Admin Mode cierra cualquier Quick Action abierta.
8. El estado de Admin Mode y producto seleccionado es React in-memory; no usa localStorage/sessionStorage/IndexedDB/cookies.
9. Write Gate V4.18A continúa `SIMULADO_BLOQUEADO`.
10. No se exponen costo, margen, USD, inversión ni proveedor interno en la proyección Storefront.

## QA ejecutado

| Verificación | Resultado |
|---|---|
| Verificación ZIP/base/bundle/HEAD | PASS |
| `git diff --check` | PASS |
| Transpilación sintáctica TS/TSX de archivos V4.18B relevantes | PASS 4/4 |
| `node --test tests/v16-v418b-storefront-admin-mode.test.mjs` | PASS 12/12 |
| `node --test tests/v16-v416-recently-viewed.test.mjs` | PASS 6/6 |
| Regresión V4.17 relevante mediante loader TS nativo | PASS; 7 links Electrodomésticos, Lavado seleccionado, Apple seleccionado |
| Regresión V4.18A dirigida | 10/12; los 2 fallos son exclusivamente el safety histórico que exige rama `v418a-*` |
| Safety V4.18B | PASS; contadores nuevos de write/red/persistencia/URL = 0 |
| Hashes Product V16 / catálogo / adapter / V4.16 / V4.17 / V4.18A | PASS 16/16 |
| Harness responsive con 320/360/390/412/768/1440 | PASS estructural |
| `npm run install:ci` | BLOCKED — exit 6: `Could not resolve host: registry.npmjs.org` |
| `npm run lint` | NO EJECUTADO válidamente; intento termina exit 127 porque `eslint` no está instalado tras bloqueo de `npm ci` |
| `npm run build` | NO EJECUTADO válidamente; intento termina exit 69 porque `vinext` no está disponible tras bloqueo de `npm ci` |
| Browser QA real 320/360/390/412/768/1440 | NO EJECUTADO; no hay app arrancable sin dependencias instaladas |
| Deploy / producción | NO EJECUTADO |

## Nota sobre regresión V4.18A

El safety scanner histórico `scripts/v418a-safety-scan.mjs` incluye una condición deliberada: la rama debe comenzar con `recovery/amarango-v16-v418a-`. Por eso dos tests históricos V4.18A fallan al ejecutarse desde la nueva rama V4.18B aunque los hashes y el Write Gate sigan intactos. El scanner V4.18B verifica explícitamente los cuatro artefactos centrales V4.18A y todos permanecen byte a byte iguales a la base.

## Responsive

Existe `public/V4.18B-Responsive-QA.html` con los anchos exactos 320, 360, 390, 412, 768 y 1440 px y medición `clientWidth / scrollWidth`. La ejecución real en navegador queda pendiente únicamente porque el runtime no puede instalar `vinext`/dependencias.

## Condición para destrabar

Reanudar desde el HEAD final de este paquete en un entorno capaz de completar `npm run install:ci`. Ejecutar, en este orden:

1. `npm run install:ci`
2. `npm run lint`
3. `npm run build`
4. `node --test tests/v16-v418b-storefront-admin-mode.test.mjs`
5. regresiones V4.18A/V4.17/V4.16 relevantes
6. `npm run safety:v418b`
7. browser QA 320/360/390/412/768/1440

Solo si todo lo obligatorio queda PASS puede emitirse `PASS FINAL — FASE H / V4.18B`.

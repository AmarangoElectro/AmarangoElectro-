# AmarangoElectro V16 — V4.17 Recovery QA

## Veredicto

`FASE F — V4.17 NAVEGACIÓN CONTEXTUAL = PASS`

- Fecha: 2026-09-04.
- Rama: `recovery/amarango-v16-v417-final-v1`.
- Commit de implementación: `95c9b2a12a6d62581bf27346c17f0ffef393c8d1`.
- Base física: `BASE-V416-SOURCE/` del transporte LIGHT-1.
- Equivalencia histórica declarada por el paquete: V4.16 `c9643e45c39b379bb69a9afc275ffe1e53862dbb`; el bundle Git histórico no está incluido y ese SHA no se presenta como verificado físicamente.
- Commit local de importación inalterada: `4286869405e143cbded247b3270de5714c286473`.
- Producción/deploy: no ejecutados.

## Resultado funcional

- helper puro fuera de `lib/catalog/`;
- taxonomía activa consumida sin cambios;
- links HTML compartibles por `sector` o `marca`;
- selección con `aria-current="page"`;
- banda server-side antes del hero y sin hueco cuando no hay opciones;
- fila duplicada de marcas de Celulares retirada; cuatro perfiles de elección preservados;
- Cliente habilitado; Asesor y Administrador sin banda V4.17;
- V4.16 “Seguí mirando” preservado byte a byte en sus tres archivos protegidos.

## QA automatizado

| Control | Resultado real |
| --- | --- |
| Instalación reproducible | PASS — Node `v24.19.0`, npm `11.9.0`, 677 paquetes |
| Lint | PASS |
| Build Vinext/Sites | PASS |
| Tests V4.16 + V4.17 | PASS — 12/12 |
| Inventory + V4.16 + V4.17 | PASS — 30/30 |
| Safety Scan V4.16 | PASS |
| Safety Scan V4.17 | PASS |
| Hashes heredados y V4.16 | PASS — 10/10 |
| Suite completa, glob físico | 254 descubiertos; 252 PASS, 2 FAIL por archivos deliberadamente excluidos del transporte LIGHT-1 |

### Reconciliación 254

La base contractual tiene 248 casos y V4.17 agrega exactamente seis: el runner descubre 254, nunca 255. Los seis V4.17 pasan.

Los únicos dos fallos de la suite completa son `ENOENT` y no fallos funcionales:

1. `V14 checkpoint remains byte-identical` requiere `public/reference/amarango-mega-home-v14-original.zip`.
2. `V4.11 outputs both QA preview and direct Android standalone without iframe` requiere `AmarangoElectro-V16-V4.11-Real-Catalog-QA-Preview.html`.

`00-LEEME-PRIMERO-CHATGPT-WORK.md` excluye expresamente esos artefactos pesados del transporte y ordena no reconstruirlos ni tocarlos. No se copiaron desde otra base ni se modificó el runner para fabricar 254/254. La misma ejecución serial con `--test-concurrency=1` dio idéntico resultado 252/2, descartando una carrera.

## QA de navegador y responsive

El preview local supervisado se abrió únicamente para QA y fue detenido.

- Storefront real en el viewport disponible: `clientWidth=1348`, `scrollWidth=1348`, overflow root `0`.
- Electrodomésticos: siete links (`Todo` + seis sectores); navegación antes del hero.
- URL directa `?sector=lavado#sector-activo`: `Lavado` seleccionado, URL y estado conservados en refresh/back/forward.
- Categoría sin sectores: cero banda.
- `/plataforma` y `/administracion`: cero banda V4.17.
- Harness portable, contenido interno exacto: 320, 360, 390, 412, 768 y 1440 px con overflow root `0` en todos.
- Mobile: track local de 777 px contenido en 304/344/374/396 px según ancho.
- Pocas opciones: 2 links; ninguna: banda oculta; V4.16 lleno/vacío: sin overflow.

El navegador controlado no permite redimensionar el viewport superior. Por eso los seis anchos exactos se ejecutaron en el harness portable dentro del navegador real; no se atribuyen falsamente al viewport superior de la aplicación.

## Cierre responsive

El baseline físico contenía `body { overflow-x: hidden; }`. Se retiró para no ocultar defectos, de acuerdo con el gate. La causa histórica del título estrecho se corrigió con `clamp(34px,10.5vw,40px)` y `overflow-wrap:anywhere`. No existe parche global de overflow en el resultado.

## STOP

V4.18, Shopping Experience Foundation y cualquier gate posterior no fueron iniciados.

# V4.18A QA — FASE G Recovery

Estado del gate: **PASS**

- Base física única: V4.17, commit `0929c7efbec946d3440f047c59fc8d32b38ac8a2`.
- Rama: `recovery/amarango-v16-v418a-quick-actions-v1`.
- Commit de implementación: `456d5e807d21b0ea76244fe6cc49f9c05514f4d6`.
- Entorno: Node y npm disponibles; dependencias instaladas con `npm ci` mediante el instalador acotado del entorno Sites.
- Producción/deploy: no ejecutados.

## QA ejecutado

| Verificación | Resultado |
|---|---|
| `npm run lint` | PASS, exit 0 |
| `npm run build` | PASS, exit 0; 8 rutas construidas |
| `node --test tests/v16-v418a-admin-quick-actions.test.mjs` | PASS, 12/12 |
| `npm run safety:v418a` | PASS; todos los contadores en 0; 10/10 hashes |
| `node --test tests/v16-v416-recently-viewed.test.mjs` | PASS, 6/6 |
| V4.17 dirigido | 5/6; único fallo esperado porque el test histórico exige literalmente la rama V4.17 anterior |
| `npm test` | 263/266; build PASS y tres fallos heredados/documentados abajo |

## Casos obligatorios V4.18A

Los 12 casos pasan: Admin-only; ausencia en Cliente/Asesor; seis acciones; precio inválido bloqueado; cuatro estados de disponibilidad; taxonomía autorizada y fail-closed; visibilidad sólo draft; foto sin upload; diff determinista; render/placeholder sin navegación; V4.16/V4.17 protegidos; safety counters en cero.

## QA de navegador local

Preview local levantado únicamente en `http://terminal.local:4173/`, sin deploy.

- Admin renderiza el disparador y las seis acciones.
- Precio `0`: error visible y aplicar deshabilitado.
- Precio `600000`: diff `$510.000` → `$600.000`.
- Aplicar: mensaje `NO AUTORIZADO EN FASE G... cero escrituras ejecutadas`.
- Harness `public/V4.18A-Responsive-QA.html`: 6/6 frames cargados y sin overflow horizontal a 320, 360, 390, 412, 768 y 1440 px.
- Sin errores originados por la app. El navegador reportó mensajes de su propia extensión de metadatos, ajenos al sitio.

## Tres fallos heredados de la suite completa

1. `v15-safety.test.mjs`: el paquete base no contiene `public/reference/amarango-mega-home-v14-original.zip`.
2. `v16-v411-real-catalog-readonly.test.mjs`: el paquete base no contiene `AmarangoElectro-V16-V4.11-Real-Catalog-QA-Preview.html`.
3. `v16-v417-subcategory-context.test.mjs`: el safety histórico pasa funcionalmente, pero el test exige `recovery/amarango-v16-v417-final-v1`; la rama actual debe ser V4.18A.

No se recrearon artefactos faltantes ni se modificó el test histórico para inventar un PASS.

## Limitación de autorización

La base no aporta Auth/RBAC físico con autoridad server-side para este editor. Por eso V4.18A es sólo una UI LAB en Admin y su Write Gate queda simulado/bloqueado. Esta superficie no concede permisos.
